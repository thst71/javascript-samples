/**
 * @typedef {import('mocha').SuiteFunction} SuiteFunction
 * @typedef {import('mocha').TestFunction} TestFunction
 */

// ... Ihre Tests

describe("IDE intgeration", () => {

    it("knows describe() and it()", () => {
        // your testcode
    });
});